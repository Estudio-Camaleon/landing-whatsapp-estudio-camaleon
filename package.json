{
  "name": "whatsapp-landing",
  "version": "1.0.8",
  "private": true,
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "dev": "vercel dev",
    "build": "",
    "start": "vercel start",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@vercel/node": "^3.0.0",
    "vitest": "^1.6.0"
  }
}
